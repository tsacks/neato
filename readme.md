# Setup instructions

1) Clone into directory
2) Create a `env.local` file to hold local settings
3) run `composer install` to install dependencies
4) run `php bin/console setup` to populate database with default data
